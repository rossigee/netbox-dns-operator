---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: netboxdnsoperators.netbox-dns-operator.rossigee.github.com
spec:
  group: netbox-dns-operator.rossigee.github.com
  names:
    kind: NetBoxDNSOperator
    listKind: NetBoxDNSOperatorList
    plural: netboxdnsoperators
    singular: netboxdnsoperator
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NetBoxDNSOperator is the Schema for the netboxdnsoperators API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetBoxDNSOperatorSpec defines the desired state of NetBoxDNSOperator
            properties:
              netboxToken:
                description: NetBoxToken is the API token for NetBox authentication
                minLength: 1
                type: string
              netboxURL:
                description: NetBoxURL is the URL of the NetBox instance
                format: uri
                type: string
              reloadInterval:
                description: 'ReloadInterval is how often to check for NetBox changes
                  (default: 5m)'
                type: string
              webhookURL:
                description: WebhookURL is the URL for NetBox webhooks (optional)
                format: uri
                type: string
              zones:
                description: Zones is the list of DNS zones to manage
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - netboxToken
            - netboxURL
            type: object
          status:
            description: NetBoxDNSOperatorStatus defines the observed state of NetBoxDNSOperator
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the operator's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              zoneStatus:
                additionalProperties:
                  description: ZoneStatus represents the status of a DNS zone
                  properties:
                    lastUpdate:
                      description: LastUpdate is when this zone was last updated
                      format: date-time
                      type: string
                    recordCount:
                      description: RecordCount is the number of DNS records in the
                        zone
                      type: integer
                    serial:
                      description: Serial is the current SOA serial number
                      type: string
                  required:
                  - recordCount
                  - serial
                  type: object
                description: ZoneStatus contains status information for each zone
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
